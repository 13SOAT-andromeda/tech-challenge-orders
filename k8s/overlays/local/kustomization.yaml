apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
  - ../../base
  - config.yaml
  - ingress.yaml

patches:
  - target:
      kind: Deployment
      name: tech-challenge-orders-api
    patch: |-
      - op: replace
        path: /spec/template/spec/containers/0/image
        value: tech-challenge-orders-api:latest
      - op: replace
        path: /spec/template/spec/containers/0/imagePullPolicy
        value: Never
