apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tech-challenge-orders-api-ingress
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tech-challenge-orders-api-svc
                port:
                  number: 80
