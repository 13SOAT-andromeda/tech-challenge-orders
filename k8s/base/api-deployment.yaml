apiVersion: apps/v1
kind: Deployment
metadata:
  name: tech-challenge-orders-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tech-challenge-orders-api
  template:
    metadata:
      labels:
        app: tech-challenge-orders-api
    spec:
      containers:
        - name: api
          image: tech-challenge-orders-api:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: api-config
            - secretRef:
                name: api-secrets
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
